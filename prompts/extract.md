Extract from the following book section. Output ONLY valid YAML, no commentary.

```yaml
principles:
  - rule: "Imperative statement of the rule"
    context: "When/where this applies"
    strength: must|should|consider

patterns:
  - name: "Pattern Name"
    when: "Situation description"
    do: "What to do"
    avoid: "What not to do"

anti_patterns:
  - name: "Anti-pattern Name"
    problem: "Why this is bad"
    instead: "What to do instead"

key_terms:
  - term: "Term"
    definition: "Concise definition"
```

Only include items that are clearly stated or strongly implied. Omit empty sections.
{extraction_notes}

---

SECTION:
{chunk}
